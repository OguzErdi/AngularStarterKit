import { PatternValidationDirective } from './pattern-validation.directive';

describe('PatternValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new PatternValidationDirective();
    expect(directive).toBeTruthy();
  });
});
